{
  "name": "bb-browser",
  "version": "0.2.0",
  "description": "Browser automation module for bbctl — uses bun:sqlite, no native deps",
  "main": "src/cli.ts",
  "bin": {
    "bb-browser": "src/cli.ts"
  },
  "scripts": {
    "postinstall": "bunx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/node": "^22.0.0"
  }
}
